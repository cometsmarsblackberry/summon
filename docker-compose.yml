services:
  web:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - .:/app
      - ./data:/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/reserve.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
